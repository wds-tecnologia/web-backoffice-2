// @ts-ignore - socket.io-client not installed, commenting out to allow build
// import { Socket, io } from "socket.io-client";

// const socket: Socket = io(process.env.REACT_APP_API_URL || "http://localhost:3333", {
//   autoConnect: true,
// });

// @ts-ignore - socket.io-client not installed
export default null as any; // Placeholder until socket.io-client is installed
